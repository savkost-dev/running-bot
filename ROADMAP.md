# DoDick — дорожная карта

> Составлено 2026-06-03. Синтез TODO из PROJECT_CONTEXT + находки сессии 0.23.0.
> Порядок внутри блоков = предлагаемый приоритет. Детали по каждому пункту — в PROJECT_CONTEXT.md.

---

## БЛОК 1. Архитектура данных (слои) — главная линия

Ради этого строилось всё остальное. Цель — промпт и формула берут данные из
единого нормализованного источника, а не «что первое попалось из сервиса».

| # | Задача | Статус | Зависит от |
|---|--------|--------|-----------|
| 1.1 | Fetch — сырьё в raw_service_data | ✅ ГОТОВО (все сервисы) | — |
| 1.2 | Enrich — производные по открытым формулам (VDOT→зоны, TSB) | ⏸ откладываем, добавляем по мере нужды | 1.1 |
| 2 | Normalize — raw → UnifiedUserData → unified_cache в БД | 🔜 СЛЕДУЮЩЕЕ | 1.1 ✅ |
| 3 | Consume — промпт/формула читают UnifiedUserData | ⬜ | 2 |
| 4 | Profile override — ручные данные + замки поверх слоя 2 | ⬜ | 2 |

**Что слой 3 закроет автоматически (сейчас костыли):**
- `_recovery_value` берёт «первое попавшееся» (recovery_score/training_readiness) — станет чётким полем
- Блок восстановления в двух вечерних промптах сейчас разный — унифицируется
- recovery_daily (суточное) vs recovery_total (длительное) — разведутся по смыслу
- пол/возраст в промпт — из единого источника

**Следующий конкретный шаг:** написать связку слоя 2 — функция читает raw_service_data,
прогоняет через data_normalizer (готов), кладёт UnifiedUserData в новую таблицу unified_cache.
data_normalizer.py уже написан и протестирован, осталось подключить к БД.

---

## БЛОК 2. Рекомендательный движок

| Задача | Суть | Зависит от |
|--------|------|-----------|
| Workout-прогноз полностью на ИИ | Сейчас формула + ИИ расходятся в основном из-за восстановления. Отдать прогноз тренировки ИИ целиком. Почти готово | желательно слой 3 |
| Калибровка формулы по ИИ | Двойной расчёт на каждого (формула + ИИ), слать настроенный, копить статистику обоих в БД, подправлять кривую формулы на реальных данных | копить данные |
| Восстановление: вечер → утро | Убрать восстановление из вечерней рекомендации совсем (за ночь меняется), оставить только в утренней | разобрать утреннюю |

---

## БЛОК 3. Утренняя рассылка — отдельная большая тема

Пока почти не трогали. Нужна отдельная сессия.
- Разобрать механику build_morning_prompt, тайминги, cache_refresh 6:45
- Утренний снапшот восстановления: на 6:45 сохранять BB/HRV/Recovery в garmin_recovery_cache
  с меткой morning_snapshot_at, чтобы вечером брать утренние значения (BB к вечеру потрачен)
- Связано с «восстановление вечер→утро» из блока 2

---

## БЛОК 4. Стабильность и сопровождение (техдолг)

| Задача | Срочность |
|--------|-----------|
| Garmin rate-limit 429 при массовом релогине — добавить задержку / не пачкой | средняя (рванёт если много токенов протухнет разом в cache_refresh) |
| Пол/ДР для user 5 (Strava-only) — ручной ввод | низкая |
| Разбивка bot.py: ✅ recovery.py + fitness.py вынесены (07.06, 5237→4724). Дальше keyboards/recommendation/schedulers — отложено | в процессе |
| Разбивка claude_advisor.py (3103 стр): план готов (фасад + реэкспорт, pace_utils первым, __getattr__ для last_prompt) — ОТЛОЖЕНО, см. PROJECT_CONTEXT | низкая |
| Дубли _pace_to_sec/_sec_to_pace (claude_advisor + data_normalizer) → utils.py | низкая |
| Дубль _zones_from_vdot (zones.py + data_normalizer) → звать zones.py | низкая |
| ✅ СДЕЛАНО: мусорные test_*/debug_/fix_db.py убраны из src и с сервера (07.06) | — |

---

## БЛОК 5. Внешние зависимости / ждём

| Задача | Статус |
|--------|--------|
| Официальный COROS API (заявка подана 02.06) | ждём одобрения. Когда дадут — ati/cti (длительная нагрузка), прогнозы recordDetailList |
| Polar Running Index (VO2max) | в профиле нет, лежит в детализации тренировки. Пока МПК вручную |
| Polar physical-info в fetch_raw | get_physical_info готова, но physical-info — событийный pull (отдаётся 1 раз). Вызывать в fetch_raw и сразу сохранять vo2max/ЛП в user_profile, иначе данные теряются |
| Мультимодельность (DeepSeek + Anthropic) | спланировано, не сделано |
| Strava: лимит атлетов 10/10 | Расширение дадут только после webhooks + deauth-чистки (письмо Strava, июль 2026). План: GET/POST /strava/webhook в oauth_server (hub.challenge при подписке; события activity и authorized=false → чистка токена), подписка через push_subscriptions API, обработка invalid refresh → деактивация, переподача с описанием изменений. Аудит токенов: scripts/probe_strava_audit.py (28.07.2026: все 10 живы, чистить нечего) |

---

## Принципы работы (зафиксировано в сессиях)

- Мелкие задачи по частям, прод трогать точечно с тестом до/после
- Деплой ВСЕГДА через .\deploy.ps1 (не одиночный scp)
- Сырьё (raw_service_data) хранить as is, парсинг отдельно (слой 2)
- Не воспроизводить проприетарные алгоритмы (Nightly Recharge, HRV Status, recoveryPct) — берём как есть
- Статичные данные (пол, ДР) — заполнять разово, не дёргать API ежедневно
