.card{
    margin:56px 0;
    border:1px solid var(--line);
    background:#fff;
    padding:26px 28px;
  }
  .card-label{
    font:600 12px/1 "IBM Plex Mono",monospace;
    letter-spacing:.14em;
    text-transform:uppercase;
    color:var(--muted);
    margin-bottom:18px;
  }
  .lane{display:flex;align-items:center;gap:14px;padding:9px 0;border-top:1px solid var(--line)}
  .lane:first-of-type{bor